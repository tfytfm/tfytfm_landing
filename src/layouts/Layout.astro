---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Today for You, Tomorrow for Me es una iniciativa de software libre que busca empoderar a jóvenes sin acceso a tecnología, mientras fomenta la colaboración e intercambio de servicios entre pequeñas empresas." />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
		<title>{title}</title>
	</head>
	<body>
		<nav class="navbar">
			<div class="nav-content">
				<a href="/" class="logo">
					<span class="text-gradient">TFYTFM</span>
				</a>
				<div class="nav-links">
					<a href="/about">¿Qué es?</a>
					<a href="/roadmap">Roadmap</a>
					<a href="/projects">Proyectos</a>
					<a href="/docs">Docs</a>
					<a href="https://discord.com/invite/VTPH7rtZfg" class="cta-button hover-float">
						Unirse
					</a>
				</div>
			</div>
		</nav>

		<main>
			<slot />
		</main>

		<footer>
			<div class="footer-content">
				<div class="footer-section">
					<h3 class="text-gradient">TFYTFM</h3>
					<p>Construyendo un futuro tecnológico más inclusivo y equitativo.</p>
				</div>
				<div class="footer-section">
					<h4>Enlaces</h4>
					<a href="#about">Sobre Nosotros</a>
					<a href="#roadmap">Roadmap</a>
					<a href="#projects">Proyectos</a>
					<a href="#docs">Docs</a>
					<a href="https://discord.com/invite/VTPH7rtZfg" class="cta-button hover-float">
						Unirse
					</a>
				</div>
				<div class="footer-section">
					<h4>Comunidad</h4>
					<a href="https://discord.com/invite/VTPH7rtZfg">Discord</a>
					<a href="https://github.com/tfytfm">GitHub</a>
				</div>
			</div>
			<div class="footer-bottom">
				<p>&copy; {new Date().getFullYear()} TFYTFM. Todos los derechos reservados.</p>
			</div>
		</footer>
	</body>
</html>

<style is:global>
	:root {
		--accent: 255, 208, 0;
		--accent-light: 255, 225, 77;
		--accent-dark: 204, 166, 0;
		--background: 13, 17, 23;
		--text: 255, 255, 255;
		--accent-gradient: linear-gradient(
			135deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			rgba(var(--accent-light), 0.3) 70%
		);
		--gray: 22, 27, 34;
		--glow: 0 0 20px rgba(var(--accent-light), 0.3);
		--max-width: 1400px;
		--navbar-height: 70px;
		--ease-out: cubic-bezier(0.22, 1, 0.36, 1);
		--duration-normal: 300ms;
	}

	html {
		font-family: 'Inter', system-ui, sans-serif;
		background: rgb(var(--background));
		color: rgb(var(--text));
		scroll-behavior: smooth;
	}

	body {
		margin: 0;
		padding-top: var(--navbar-height);
		min-height: 100vh;
		overflow-x: hidden;
	}

	.navbar {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		height: var(--navbar-height);
		background: rgba(var(--background), 0.8);
		backdrop-filter: blur(10px);
		border-bottom: 1px solid rgba(var(--accent-light), 0.1);
		z-index: 1000;
	}

	.nav-content {
		max-width: var(--max-width);
		margin: 0 auto;
		padding: 0 2rem;
		height: 100%;
		display: flex;
		align-items: center;
		justify-content: space-between;
	}

	.logo {
		font-size: 1.5rem;
		font-weight: 800;
		text-decoration: none;
		color: rgb(var(--text));
	}

	.nav-links {
		display: flex;
		gap: 2rem;
		align-items: center;
	}

	.nav-links a {
		color: rgb(var(--text));
		text-decoration: none;
		font-weight: 500;
		transition: color var(--duration-normal) var(--ease-out);
	}

	.nav-links a:hover {
		color: rgb(var(--accent));
	}

	.cta-button {
		background: rgb(var(--accent));
		padding: 0.5rem 1.5rem;
		border-radius: 50px;
		color: rgb(var(--background)) !important;
		font-weight: 600;
	}

	main {
		min-height: calc(100vh - var(--navbar-height));
	}

	footer {
		background: rgba(var(--gray), 0.3);
		padding: 4rem 0 1rem;
		margin-top: 4rem;
	}

	.footer-content {
		max-width: var(--max-width);
		margin: 0 auto;
		padding: 0 2rem;
		display: grid;
		grid-template-columns: 2fr 1fr 1fr;
		gap: 4rem;
	}

	.footer-section {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.footer-section a {
		color: rgba(var(--text), 0.8);
		text-decoration: none;
		transition: color var(--duration-normal) var(--ease-out);
	}

	.footer-section a:hover {
		color: rgb(var(--accent));
	}

	.footer-bottom {
		text-align: center;
		margin-top: 3rem;
		padding-top: 1rem;
		border-top: 1px solid rgba(var(--accent-light), 0.1);
	}

	@media (max-width: 768px) {
		.nav-links {
			display: none;
		}

		.footer-content {
			grid-template-columns: 1fr;
			gap: 2rem;
		}
	}

	/* Mantener los estilos globales existentes */
	.text-gradient {
		background: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
	}

	.hover-float {
		transition: transform var(--duration-normal) var(--ease-out),
					box-shadow var(--duration-normal) var(--ease-out);
	}

	.hover-float:hover {
		transform: translateY(-2px);
		box-shadow: var(--glow);
	}
</style>
